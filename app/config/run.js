(function() {
	'use strict';

	/**
	 * @name ng-demo
	 * @description
	 *
	 * Run phase of the Application.
	 */
	angular
		.module('ng-demo')
		.run(run);

	function run($state, $rootScope) {

	}
})();
